<!DOCTYPE HTML>
<html>
<head>
    <meta charset="utf-8">
</head>
<body>
<ul>
    {% for article in articles %}
        <li>{{ article.titre }} - {{ article.contenu }} -
            {% if article.etoiles >= 4 %}
                Les meilleurs
            {% elseif article.etoiles == 2 or article.etoiles == 3 %}
                Les pas trop mal
            {% else %}
                Non recommandés
            {% endif %}
                - {{ loop.index }} - 
                <img src="{{ article.image }}">
        </li>
    {% endfor %}
        {{ test }}
</ul>


</body>
</html>